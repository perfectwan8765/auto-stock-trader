# Phase 1 개발환경 (M1 + pyenv 3.10.13, .venv)
# 설치 순서가 중요: 선행 핀 → pyqlib → xgboost
# 사전: brew install libomp  (LightGBM OpenMP 런타임 의존성)

# --- 선행 핀 (Qlib 호환: numpy<2, pandas<2.2, cython<3) ---
numpy==1.26.4
cython==0.29.37
pandas==2.1.4

# --- Qlib 스택 ---
pyqlib==0.9.7
lightgbm==4.6.0
xgboost==3.2.0
scikit-learn==1.7.2

# --- 토스 어댑터(Phase 0·5) ---
requests>=2.31,<3
python-dotenv>=1.0,<2

# --- 데이터 파이프라인(Phase 2) ---
# yfinance: S&P500 일봉 수집 (비공식 스크래퍼 — 개선9 재시도·폴백 필요)
yfinance==1.5.1
# vendor/dump_bin.py 직접 의존 (pyqlib 딸림이지만 명시 핀)
fire==0.7.1
loguru==0.7.3
tqdm==4.68.4

# --- 대시보드(백테스트·주문 뷰어) ---
# mlruns pkl·execution_logs 읽어 Streamlit 웹 화면 렌더. 내장 차트만 사용(추가 뷰 라이브러리 없음).
streamlit==1.59.2

# --- 테스트 ---
# src/toss 단위테스트(mock 응답). 개선10/13 예외·redact 회귀 방지.
pytest==9.1.1
