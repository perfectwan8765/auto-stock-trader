# secrets — 절대 커밋 금지
.env
.env.local
*.pem
*.key

# 토큰/캐시 (자격증명 파생물)
.cache/
.toss_token.json

# 실측 결과 원본(계좌식별자·잔액·보유 등 민감정보 포함 가능) — 커밋 금지.
# 계정 무관한 API 계약·비용만 추려 qlib-toss.md §Phase 0 실측 상수에 남긴다.
phase0b-execution-gate.md

# python
.venv/
venv/
__pycache__/
*.pyc
# .python-version 은 재현성 설정이므로 커밋한다 (gitignore 하지 않음)

# qlib 데이터/산출물
data/
mlruns/
*.bin
# Phase 4 시그널(생성물, 매 실행 재생성)
signals/
# 리밸런싱 주문로그(dry-run·실발주 산출물)
execution_logs/

# 작업계획서(로컬 전용, 추적 안 함)
docs/

# 에이전트 지침(로컬 전용)
CLAUDE.md

# OS/IDE
.DS_Store
.vscode/
