# 사전등록 — Edge v2: 미국 소형주 + Insider(Form 4)

> ## 상태: 확정 — 커밋으로 효력 발생
>
> **이 파일은 커밋되는 순간 구속력을 갖는다.** 사전등록의 효력은 "결과를 보기 전에 정했음"을
> **커밋 타임스탬프로 증명**하는 데서 나온다. 미정 항목 **0건**으로 채운 뒤 커밋한다.
>
> **커밋 시점 = 단계 1 전수 확정 직후.** 주 표본을 DERA 13분기(2023q1~2026q1)로 완성하고
> 커버리지·퇴출·클러스터·ADDV·검정력·스프레드를 전수로 다시 잰 값이 아래에 들어 있다.
> **어떤 이벤트 조건부 수익률(CAR)도 아직 계산하지 않았다.**
>
> ⚠️⚠️ **§3.3(D-1)과 §3.4(D-2)는 이탈 기록이다.** 둘 다 **제약을 느슨하게 하는 방향**이며
> 사용자 승인을 받았다. 커밋 후에도 삭제하지 않는다.
>
> 작업계획서: `docs/microcap-insider-workplan.md` (git 추적 밖. 전수 숫자는 그 문서 §3.15)
> 상위 계획: `qlib-toss.md` · 구조·현황: `README.md`

- 작성일: 2026-08-09
- 확정일: **2026-08-15** (단계 1 전수 확정 후)
- 확정자: **SANGWAN**

---

## 0. 무엇을 검정하는가

미국 소형주(small/micro-cap)에 대한 내부자 공개시장 매수(SEC Form 4, 거래코드 `P`)가,
비용·편향·집행제약을 정직 반영한 뒤에도 **실현 가능한 selection alpha**를 갖는지 판정한다.

판정은 **2층**으로 나뉜다.

| 층 | 묻는 것 | 표본 | 유의성 요구 |
|---|---|---|---|
| **1층** | 실현 가능한 신호가 **존재**하는가 | **주 스펙 유니버스 2,355건 / 457종목** (전 시장 14,136이 아니다 — §1.2 필터 전부 적용 후. 실측 구간 2023-01-03~2026-03-31 = 3.24년) | **요구함** (아래 §5) |
| **2층** | $700~$100k에서 **집행 생존**하는가 | walk-forward 포트폴리오 | **요구하지 않음** |

**최종 게이트(2층)는 단 1회만 관측한다.**

---

## 1. 이벤트 정의

### 1.1 고정 — 협상 대상 아님

| 항목 | 값 |
|---|---|
| 데이터 원천 (백테스트) | SEC DERA Insider Transactions Data Sets, 분기 TSV |
| 데이터 원천 (실시간) | EDGAR 직접 폴링 |
| 거래코드 | `TRANS_CODE == "P"` (공개시장 매수) |
| 취득/처분 | `TRANS_ACQUIRED_DISP_CD == "A"` |
| 파생 | `DERIV_TRANS` **제외** |
| **이벤트 집계 단위** | **(발행사 CIK, `FILING_DATE`)** — 거래행 단위 금지(2.6배 과대) |
| **진입 기준 시각** | **`FILING_DATE`** — `TRANS_DATE` 진입은 미래 누수 |
| 장중/장마감 구분 | 불가(DERA에 acceptance time 없음) → **항상 t+1 진입 가정** |
| 티커 정제 | 공백·`N/A`·`NONE`·`/` 포함 값 제외 |
| 증권 유형 | `securityType == STOCK` (ETF·폐쇄형펀드 배제) |

### 1.2 확정해야 하는 필터

| 축 | 값 | 근거 / 잔존율 |
|---|---|---|
| 거래금액 하한 | **≥ $10,000** | 잔존 77.3%. p10이 $10이라 하한 필수. $50k는 대안 #9 |
| 주가 하한 | **≥ $5** | 잔존 72.4%. $1이면 91.6%지만 OTC·핑크시트가 섞이고 스프레드가 극단이 된다 |
| 주가 상한 | **≤ $50** | **집행 제약 예비 조치.** 근거는 **단계 3.5** 실주문에서 완결된다. 데이터에는 이미 적용돼 있음. 1층에는 불필요하나 해제하면 **1층/2층 표본이 달라져** "1층에서 산 신호를 2층에서 실행할 수 없다"는 조용한 실패가 생긴다 → 유지 |
| 공시지연 상한 | **≤ 5영업일** | 잔존 91.0%. **가장 싼 필터.** `FILING_DATE − TRANS_DATE` 직접 계산 — `TRANS_TIMELINESS`는 99%가 빈값이라 못 쓴다 |
| 10b5-1 제외 | **제외** | `AFF10B5ONE`. **2023년 이후 분기에만 존재** — 주 표본(2023~2026) 전체에 적용 가능하다. 이전 구간과 비교할 때는 필터 끈 버전을 병기한다 |
| **ADDV 유동성 하한** | **≥$200,000** ✅확정 | 전수 측정 — 잔존 **70.4%**(2,355/3,343, 457종목), t=3.69. (A-1 정정 반영) 검정력은 $500k까지 통과하므로 결정 근거는 **원논문 재현성 + 저유동에서 EDGE/CS/AR 전부 편의**. $100k는 대안 #14. ★ **$700 계좌에선 시장충격 0.02%로 무관** — ADDV의 본래 역할은 capacity 곡선 x축(§6) |
| **시총 하한 (PIT)** | **하한 없음 + NANO(<$50M) 층화 보고** ✅확정 | 전수 기준 ADDV ≥$200k가 NANO를 **12.3% → 5.6%** 로 걸러낸다. 살아남은 것이 "거래는 되는데 기관이 못 들어가는" 구간의 가장 순수한 표본이며 §0의 논거가 겨냥하는 지점이다. 추가 하한의 대가는 5%대인데 검정력은 거의 안 변한다 → **논거가 결정.** 단 회전율 이상 신호(`<$25M`의 ADDV 통과율 45.7%가 `$25~50M`의 17.3%보다 높다)가 있어 **배제 대신 층화 보고**. NANO 층화는 §4의 N에 계상하지 않는다 — 판정은 전체 표본으로만 한다 |
| 시총 상한 (PIT) | **$300M** ✅확정 | PIT 시총 = (filed ≤ 이벤트일 최신 발행주식수) × 이벤트일 종가. 전수 3,569 이벤트 / 571 종목(ADDV 적용 전). 검정력은 $200M부터 충분해 컷은 논거로 결정. $500M은 대안 #13 |
| `float_scaled` 포함 | **제외** ✅확정 | 시총 출처 중 `float_scaled`는 전수 14.0%다. `float/시총` p50=0.88·p75=1.18로 이론상 1을 넘을 수 없는 값이 넘는다(10-K 표지 기준일과 최대 ~18개월 간극). 오차 ±2배가 **대형주를 마이크로캡 바구니로 밀어넣는 방향**이라 §0 논거를 훼손한다. 포함 버전은 대안 #12 |
| 클러스터 (주 스펙 = **무제약**. 아래는 대안 #8의 정의) | **공시일 동일 + 고유 내부자 ≥2명** ✅확정 | 전수 측정 — **35.9%**(≥3명 15.8%). 이전 판의 "약 17%"는 **`accession`(공시 건수) 기준 오류**였다(하나의 Form 4에 복수 reporting owner 가능). 고유 `RPTOWNERCIK` 수로 세야 한다. t≈3.17. **≥3명(16.2%)은 t=2.05로 미달** → 대안 #8은 ≥2명으로 |
| **이상치 처리** | **winsorize 1% 양측** ✅확정 | ★ **이 문서에서 가장 큰 자유도.** 마이크로캡 30일 수익률 첨도 4,131 → 규칙 하나로 t가 1.77↔8.45. **raw로는 어느 시총 컷에서도 `t>3.0` 미달.** 변경은 시도 1회 추가가 아니라 **이 사전등록의 재작성 사유** |

### 1.4 동시 신호 선택 규칙 — 주 스펙 (★6차 신설)

동시 신호가 수용량의 17~50배이므로 **무엇을 고를지가 성과를 좌우한다.** 사후 자유도가 되지 않도록 전부 고정한다.

| 항목 | 값 |
|---|---|
| **지표** | `price deviation` |
| **정의** | `(공시일 종가 − 거래단가) / 거래단가` |
| **측정 시점** | 공시일 `t`까지의 종가로만 산출 (원논문: *"features … are calculated using closing prices up to t"*) |
| **방향** | **값이 클수록 우선.** 원논문 Table 4: `≤0% → CAR 2.3%` … `>10% → CAR 6.3%` |
| **동점 처리** | 거래금액이 큰 쪽 |
| **선발 수** | 계좌 수용량 (초기 20) |

⚠️ ~~52주 고가 대비 거리~~는 **주 스펙에서 뺐다.** 원논문 피처 기여도 1위(0.360)지만
**부호 없는 중요도**이고 **정의(수식)도 논문에 없다.** 방향을 추측해 넣으면 그 자체가 자유도다.
또한 **원논문은 선택 규칙 자체를 제시하지 않는다** — 모든 피처를 GBM에 넣고 중요도는 사후 산출이다.
**따라서 이 선택 규칙은 문헌의 규칙이 아니라 우리가 만든 것이다.**

### 1.3 내부자 유형 축

| 축 | 값 |
|---|---|
| 주 스펙 | **임원 + 이사** ✅확정 — **조작적 정의**: `RPTOWNER_RELATIONSHIP`이 `Officer` 또는 `Director`를 **포함**하면 채택(복합 문자열 포함). **`TenPercentOwner` 단독은 제외** |
| 대안으로 검정할 것 | 임원만 / CEO·CFO만 (§4의 시도 예산에 계상) |

---

## 2. 표본 구간

| 구간 | 이벤트 커버리지 (전수) | 역할 | 확정 |
|---|---:|---|---|
| **2023 ~ 2026** | **84.5%** | **주 표본 — 1층·2층 판정** | ✅ |
| 2020 ~ 2022 | 64~65% (표본) | 스트레스 보조 — 2022 약세장·2020 COVID | ✅ |
| 2016 ~ 2019 | 48~54% (표본) | 결론 근거 **불가**. 감쇠 방향 확인용만 | ✅ |

- **커버리지 임계**: **80%** ✅확정 — 주 표본 전체 84.5%로 통과
- **커버리지 정의**: 이벤트 가중 커버리지. **모집단을 반드시 병기**한다
- ⚠️ **판정은 구간 전체 커버리지로 한다.** 2023~2024의 개별 분기는 77~85%로 임계 언저리다
  (2023Q1 79.0% · 2024Q4 78.4% · 2025Q4 96.3% · 2026Q1 96.9%). 분기별 임계를 걸면
  주 표본이 2025년부터가 되어 표본이 3분의 1로 줄고 1층 검정력이 무너진다
- ⚠️ **실측 구간은 2023-01-03 ~ 2026-03-31 = 3.24년**이다. **DERA 2026q2는 미발행**(2026-08-15 확인)이라
  "3.5년"을 쓰면 안 된다. 아래 §5.2.1의 t는 **환산 없이 실측 n**으로 냈다
- **walk-forward 분할** ✅확정 — **확장창, 6개월 스텝, 4폴드**:

| 폴드 | 학습(확장) | 검정 |
|---|---|---|
| 1 | 2023-01-03 ~ 2024-06-30 | 2024-07-01 ~ 2024-12-31 |
| 2 | 2023-01-03 ~ 2024-12-31 | 2025-01-01 ~ 2025-06-30 |
| 3 | 2023-01-03 ~ 2025-06-30 | 2025-07-01 ~ 2025-12-31 |
| 4 | 2023-01-03 ~ 2025-12-31 | 2026-01-01 ~ 2026-03-31 (3개월) |

  DERA가 분기를 추가 발행하면 **폴드를 뒤에 잇는다** — 앞 폴드의 경계는 재조정하지 않는다.

---

## 3. 결측(생존편향) 처리 — partial identification

결측 이벤트는 버리지도 채우지도 않고 **경계로 구간을 내어 보고**한다(Manski partial identification).

> ⚠️ **2026-08-09 3차 실측으로 이 절의 축이 교체됐다.** 이전 판은 **종국적 운명**(피인수/파산)을
> **보유기간 수익률**의 경계로 썼는데, 그 운명은 평균 1.5~2년 뒤에 일어난다.
> 지금의 축은 **"보유기간 안에 퇴출됐는가"** 다.

### 3.1 실측 — 퇴출 시점 (결측 티커 **435개 전수** EDGAR 조회 · 결측 이벤트 2,203)

| 신호 | n | ≤30일 | ≤60일 | ≤90일 | p50 |
|---|---:|---:|---:|---:|---:|
| 인수 공시 (DEFM14A/PREM14A/SC 14D9) | 740 | 1.4% | 3.0% | **4.7%** | **518일** |
| 폐지 (Form 25/25-NSE) | 1,315 | 2.1% | 4.0% | **5.1%** | 468일 |
| 파산 (8-K Item 1.03) | 81 | 1.2% | 1.2% | **1.2%** | 571일 |
| 마지막 공시 | 2,182 | 0.4% | 0.7% | 0.9% | **730일** |

**결측 이벤트의 96.9%(2,135/2,203)는 보유기간 90일 안에 아무 퇴출 신호도 없다.**
마지막 공시 p50이 730일이라는 것은 결측 기업 대부분이 이벤트 후 2년 가까이 SEC에 계속 공시하고 있었다
= 보유기간 동안 정상적으로 거래되고 있었다는 뜻이다. 결측 원인은 회사의 죽음이 아니라
**Yahoo가 나중에 이력을 통째로 삭제한 것**이다.

★ **독립 소스 교차검증.** 가격 결측 티커 중 Toss 메타 보유 309개의 **60.5%가 Toss 쪽에서도 `DELISTED`**
인 반면 가격 확보 티커 1,908개는 **0.3%** 뿐이다. 결측은 퇴출과 강하게 결합돼 있다 —
**요점은 그 퇴출이 보유기간 안에 오지 않는다는 것**이다.

### 3.2 경계 — 확정

```
E[결측] = 0.031 × (−55%) + 0.969 × (관측 CAR × h)
          └ 보유기간 내 퇴출     └ 그 기간 살아 있던 결측
```

- 보유기간 내 퇴출 비율: **3.1%** (68/2,203, H=90 기준 전수)
- 파산 손실 가정: **−55%** (Shumway 보정값. 민감도로 −30%/−100% 병기)
- **판정 haircut `h = 0`** ✅확정 — 가장 보수적. 주 표본이 h=0에서도 임계를 넘으므로
  더 약한 가정을 끌어올 이유가 없다
- 보고는 **h = 0 / 0.5 / 1.0** 세 값 모두. **판정은 h=0으로만**

관측 CAR 4%(예시 상수) 기준 `보정 CAR = 커버리지 × CAR + (1−커버리지) × E[결측]`,
주 표본 커버리지 **84.5%**:

| h | E[결측] | 주 표본 (84.5%) |
|---|---:|---:|
| 1.0 | +2.17% | 3.72% |
| 0.5 | +0.23% | 3.41% |
| **0.0 (판정)** | **−1.71%** | **3.12%** |

### 3.3 ⚠️⚠️ 사전등록 이탈 기록 (Deviations Table) — **승인됨 (2026-08-15)**

형식은 [Willroth & Atherton (2024, *AMPPS*)](https://journals.sagepub.com/doi/full/10.1177/25152459231213802)의
**Preregistration Deviations Table**을 따른다. 범주 어휘도 그 논문에서 온 것이다.

| 필드 | 값 |
|---|---|
| **No.** | D-1 |
| **Type** | `inferential criteria` (판정 기준) |
| **Reason** | `preregistered plan not possible/inappropriate` |
| **Timing** | `after data were accessed but before results of preregistered approach were known` |
| **Original wording** | "인수 공시 간격의 **p50이 90일을 넘으면** 프리미엄 가정이 0으로 내려가고 비관 경계가 주 표본에서도 임계 미달 → **프로젝트 종료**" |
| **측정 결과** | p50 = **562일**(표본) / **518일**(전수) → **글자 그대로는 kill이 발동한다** |
| **Deviation description** | 조건이 잘못 명세됐다고 판단해 교체. 신 조건 = "보유기간 안에 퇴출 신호가 있는 결측 이벤트 비율이 **15%** 를 넘으면 종료" → 전수 측정값 **3.1%** 로 통과 |
| **Reader impact** | **제약이 완화됐다.** 구 조건을 글자대로 적용했다면 프로젝트가 종료됐을 것이다. **이 이탈이 없었다면 이후의 모든 결과가 존재하지 않는다** |
| **정정 시점** | 2026-08-09 — timing 측정 직후 · 어떤 CAR 계산 전 · **이 파일 커밋 전** |
| **승인** | 2026-08-15, 사용자 승인. 대안(구 조건 복원 = 프로젝트 즉시 종료)을 함께 제시한 뒤의 선택이다 |

**방어 근거 — 강한 순서대로**

1. ★ **이 사전등록이 아직 커밋되지 않았다.** [COS](https://www.cos.io/blog/preregistration-plan-not-prison)는
   분석 시작 전이라면 새 사전등록을 만드는 것을 *"free and encouraged"* 라 한다. **가장 단단한 근거**
2. **시점 범주가 문헌에 실재한다** — *"after data were accessed but before results of preregistered
   approach were known"* 는 Willroth & Atherton의 독립 범주다
3. **"계획이 부적절했음"은 인정된 이탈 사유 범주**다
4. 정정 논리가 결과와 독립: **"운명 시점 ≠ 보유기간"은 CAR을 몰라도 성립한다**

**방어할 수 없는 부분 — 반드시 함께 읽을 것**

1. ★★ **COS가 긋는 선은 우리가 가정한 것보다 앞이다** — *"data analysis ... **includes calculating
   summary statistics**"*. 퇴출 시점 분위수·ADDV·클러스터 분포를 계산했으므로 **그 문구 기준으로는
   이미 분석 시작 이후**다. 완화 논거("결과변수가 아니라 공변량")는 **문헌의 문구가 아니라 우리 해석**이다
2. **시점 범주는 면죄부가 아니다** — *"data-dependent deviations increase risk of bias"*
3. **제약 완화 방향의 이탈을 별도로 허용하는 규정은 없다**
4. **구 조건이 발동한 직후에 수정됐다는 사실은 지워지지 않는다**

> **이 표는 커밋 후에도 삭제하지 않는다.** OSF amendment 관행은 원본을 날짜와 함께 남기고
> 개정을 별도 타임스탬프로 기록한다. 근거 조사: `docs/prereg-deviation-and-attrition-research.md`
>
> 참고: 사전등록 연구의 **82%가 이탈하고 55%만 보고**한다. **이탈이 실패가 아니라 미보고가 실패다.**

### 3.4 ⚠️ 사전등록 이탈 D-2 — 스프레드 추정량 합격선 (2026-08-15)

| 필드 | 값 |
|---|---|
| **No.** | D-2 |
| **Type** | `measurement` (측정 절차) |
| **Reason** | `preregistered plan not possible/inappropriate` — 어떤 추정량으로도 만족 불가 |
| **Timing** | `after data were accessed but before results of preregistered approach were known` (CAR 계산 전) |
| **Original wording** | "EDGE 스프레드가 유니버스 종목별로 산출되고 음수 비율을 함께 보고. **합격선: 음수 비율 ≤10%.** 초과하면 그 추정량은 이 유니버스에서 못 쓰는 것으로 보고 CS/AR과 대조해 대체 추정량을 고른다" (작업계획서 §5 단계 1 검증) |
| **측정 결과** | 주 스펙 표본 n=2,356에서 **세 추정량 × 네 창(30/60/120/252) 전부 미달**. 음수 비율 — EDGE 29.4%/22.6%/19.3%/**14.7%** · CS 53.1%/51.2%/50.0%/51.9% · AR 35.0%/29.6%/23.2%/16.2%. **CS는 양수 중앙값이 0.00%로 붕괴**한다 |
| **Deviation description** | 지시대로 CS·AR과 대조했으나 **대체할 것이 없었다** — EDGE가 전 구간에서 최선이다. 따라서 추정량이 아니라 **합격선을 음수 처리 규칙으로 교체**한다. 부과값 `spread_final` = **EDGE 창 252거래일**, 음수·결측은 **같은 ADDV 십분위의 양수 추정 중앙값**으로 대체 |
| **왜 0 클립이 아닌가** | 음수는 "스프레드가 0"이라는 정보가 아니라 **추정 실패**다. 0을 부과하면 비용이 과소 계상되고 그 방향은 **1층 통과를 쉽게 만드는 쪽**이라 "비관 경계로 판정한다"는 §3.2 원칙과 정면 충돌한다 |
| **Reader impact** | **제약이 느슨해졌다.** 합격선을 글자대로 적용했다면 이벤트별 스프레드 부과 자체가 불가능해지고 §5.2의 격자 판정으로 후퇴했을 것이다. 다만 대체 규칙은 음수를 **0이 아니라 양수 중앙값**으로 채우므로 부과 비용은 0 클립보다 **크다**(주 스펙 p50 1.11% vs EDGE 원값 1.05%) |
| **승인** | 2026-08-15, 사용자 승인 |

⚠️ **첫 측정에서 음수 0.0%가 나왔던 것은 측정 오류다.** `bidask.edge`의 기본값 `sign=False`가
`sqrt(|s2|)`를 돌려주기 때문이며, 부호를 지운 값으로는 합격선 판정이 성립하지 않는다.
**`sign=True` 필수** — 재현 시 이 인자를 확인할 것.

---

## 4. 시도 예산 N

**N = 14** ✅확정. 아래 14개를 열거로 고정한다.

| # | 스펙 | 바꾸는 축 |
|---:|---|---|
| 1 | **주 스펙** — H=30 · 진입 t+1 시가 · 임원+이사 · 금액≥$10k · 주가 $5~$50 · 지연≤5일 · **시총<$300M** · **`float_scaled` 제외** · **ADDV≥$200k** · 클러스터 무제약 · **선택규칙 = `price deviation` 상위**(정의는 §1.4) · **winsor 1% 양측** | — |
| 2 | H = 20일 | 호라이즌 |
| 3 | H = 60일 | 호라이즌 |
| 4 | H = 90일 | 호라이즌 |
| 5 | 진입 = t+1 종가 | 진입 시점 |
| 6 | 내부자 = 임원만 | 내부자 유형 |
| 7 | 내부자 = CEO/CFO만 | 내부자 유형 |
| 8 | **클러스터 변형** — 고유 내부자 ≥2명 (전수 35.9%) | 이벤트 정의 |
| 9 | 금액 하한 $50k | 규모 필터 |
| 10 | 선택규칙 = 내부자 직급 우선(임원 > 이사) | 선택 규칙 |
| 11 | 선택규칙 = 매수금액 상위 | 선택 규칙 |
| 12 | `float_scaled` 포함 (주 스펙은 제외) | 유니버스 |
| 13 | **시총 컷 $500M** — 원논문 범위($30M~$500M) 재현 | 유니버스 |
| 14 | **ADDV 하한 $100k** — 논거 잠식률 측정용 (주 스펙 $200k) | 유니버스 |

### 4.1 세는 규칙

- **1회 = 하나의 `(호라이즌 × 표본정의 × 선택규칙)` 조합**에 1층 판정을 내리는 것
- **세지 않는 것** — ① §3.2의 haircut 3값(h=0/0.5/1.0) ② 스프레드 격자 ③ 3갈래 진입 중 참고치 2개
  ④ raw 병기값(판정은 winsor 1%로만).
  **이유: 고를 수 없기 때문이다.** 판정이 비관 셀 하나로 미리 묶여 있으므로 자유도가 생기지 않는다.
  유리한 셀을 고를 수 있게 되는 순간 이 면제는 무효다
- **세는 것** — 위 표에 없는 조합 전부. 그 경우 **N을 재신고하고 다시 커밋**한다
- ⚠️ **이상치 규칙 변경은 "1회 추가"가 아니다.** §1.2에서 winsor 1%로 고정했으므로 다른 수준을
  시도하려면 N 재신고가 아니라 **이 사전등록을 다시 작성**해야 한다. 규칙 하나로 t가 4.8배 움직이므로
  시도 예산으로 흡수시킬 수 없다

### 4.2 N 리셋 근거

대형주 실험에서 소진한 시도(LightGBM·GRU·다중시드·호라이즌 프로브)는
**유니버스·신호·라벨이 전부 다르므로 별개 family로 리셋**한다.
근거를 여기 남겨야 사후 자기변명이 아니게 된다.

---

## 5. 검정 방법 — 1층

| 항목 | 값 |
|---|---|
| 산출물 | **BHAR**을 **3갈래**로: `t=0 종가`(이론 참고) / **`t+1 시가`(판정)** / `t+1 종가`. 청산은 `t+1+H 종가`. 호라이즌 단위는 **거래일**. 가격은 `data/qlib_us_small`의 **배당·분할 조정 수익률** (§9 A-2) |
| 비용 부과 | 시장가 스프레드를 **진입·청산 양쪽에 전액** 부과 → "실현 가능 CAR" |
| 스프레드 추정량 | **EDGE 창 252거래일** ✅확정 (Ardia·Guidotti·Kroencke, JFE 2024). `bidask.edge_rolling(..., sign=True)` — **기본값 `sign=False`는 부호를 지우므로 금지**. CS·AR 대조는 완료했고 EDGE가 전 구간 최선이었다(§3.4) |
| **스프레드 부과 방식** | **각 이벤트에 그 종목의 `spread_final`**(= EDGE w252, 음수·결측은 같은 ADDV 십분위 양수 중앙값 대체 — §3.4). 횡단면 분위(p50/p90) 고정 금지 — 단일 분위는 "항상 최악 종목만 거래"를 가정하므로 비관이 아니라 틀린 가정이다. **주 스펙 부과 분포: p25 0.86% · p50 1.11% · p75 1.56% · p90 2.22% · 평균 1.32%** |
| **이상치 처리** | **winsorize 1% 양측 고정 — 적용 지점은 이벤트별 BHAR 횡단면**(§9 A-2). raw 값은 참고로 병기하되 **판정은 winsor 1%로만** |
| 위험조정 | FF3/5 + MOM, **+ IWC**, rf 차감, EDGAR SIC 섹터 통제. ~~시장 국면 더미~~는 정의가 없어 **삭제**(§9 A-2) |
| 시계열 자기상관 | Newey-West HAC |
| **횡단면 교차상관** | **BMP / Kolari-Pynnönen 보정 t — 이것이 주 판정** |
| 교차확인 | calendar-time portfolio (Fama 1998). **결론이 어긋나면 보수적인 쪽 채택** |
| 다중검정 보정 | Bonferroni/BH (t-stat이므로 DSR 아님) |
| 층화 보고 | 공시 시점 모멘텀 버킷별 CAR을 **반드시 분리 보고** |

> ⚠️ **Newey-West HAC만으로는 부족하다.** HAC는 시계열용이고, 같은 날짜 이벤트들의
> abnormal return 상관은 횡단면 문제다. 쌍상관 0.02·100종목이면 t가 약 1.73배 부풀려지며
> 우리 동시 포지션은 H=30에서 중앙값 339개다. **미보정 t로 판정하면 kill gate가 작동하지 않는다.**

### 5.1 t 하한

| 기준 | 값 |
|---|---|
| Bonferroni | α = 0.05 / 14 = **0.0036** → 양측 **t ≈ 2.92** |
| 문헌 기준 | **t > 3.0** (Harvey-Liu-Zhu 2016 — 내부자거래는 이미 공표된 anomaly) |
| **적용** | **둘 중 엄격한 쪽 = `t > 3.0`** ✅확정 |

### 5.2 1층 임계 — 2층 요구 net에서 역산

```
CAR_gross = annual_net × H / 252 + c_rt
c_rt(왕복) = 수수료 0.10% × 2 + 스프레드 s × 2
```

- 목표 net: **10%/yr** ✅확정. 4%는 폐기됐다 — 소형주 지수 장기 기대수익(≈8%)보다 낮아
  **IWC를 그냥 사는 것보다 못하고**, 1층을 통과하고도 2층에서 반드시 죽는 구간을 열어두게 된다

- **스프레드를 한 점에 고정하지 않는다.** 임계는 **스프레드별 격자**로 선언한다:

| 편도 s | H=30 | H=60 | H=90 |
|---|---:|---:|---:|
| 0.23% | 1.9% | 3.0% | 4.2% |
| 0.80% | 3.0% | 4.2% | 5.4% |
| 1.50% | 4.4% | 5.6% | 6.8% |
| 2.00% | 5.4% | 6.6% | 7.8% |

- **판정 대상**: 실현 가능 CAR의 **비관 경계 = §3.2의 `h = 0` 행** — 이론 CAR과 `h=0.5/1.0`은 참고치.
  (~~"비관 세트"~~는 폐기된 사유별 4시나리오 시절 용어다)
- **적용 스프레드**: **이벤트별 종목 EDGE 추정치** ✅확정. 위 격자는 **감도 표시용**이며 판정 기준이 아니다.
  실현 비용 분포(p50·p90·평균)를 함께 보고해 격자 어느 행에 해당하는지 드러낸다
- **호라이즌을 섞지 말 것** — H=60 칸에 대조할 값은 60일 CAR이다

### 5.2.1 검정력 — 위 임계를 넘길 표본이 있는가

**★v3.4 전수 재계산** (`measure_power.py`). `CAR_true = 3.0%` · **실측 n(3.24년, 환산 없음)** ·
교차상관 SE 팽창 1.7배 가정 · σ는 주 스펙 유니버스 457종목의 30거래일 무조건부 수익률
342,055관측에서 산출:

```
n_eff = n_events(주 스펙 필터 전부 적용)      SE = σ / √n_eff      t = CAR_true / (SE × 1.7)
```

| 이상치 규칙 | σ | $200M | **$300M (주 스펙)** | $500M |
|---|---:|---:|---:|---:|
| raw | **50.0%** | t=1.29 ❌ | t=1.71 ❌ | t=2.09 ❌ |
| **winsor 1%** | **23.2%** | t=2.79 ❌ | **t=3.69 ✅** (n=2,355) | t=4.51 ✅ |
| winsor 2.5% | 20.6% | t=3.14 | t=4.16 | t=5.08 |

- **주 스펙 t = 3.69** — `t > 3.0` 통과. (표본 추정 3.96보다 낮다: σ가 20.7% → 23.2%로 오르고
  3.5년 환산이 사라졌다)
- **raw로는 어느 컷에서도 `t>3.0`을 못 넘는다** → §1.2의 이상치 규칙은 성립 조건이다
- **검정력은 시총 컷을 결정하지 못한다** — 컷은 논거로 정했다($300M)
- ⚠️ **1.7배 팽창은 문헌 예시치**다. 단계 3에서 실측하고 **2배를 넘으면 t=3.69가 흔들린다** —
  그 경우 이 표를 갱신하고 판정 전에 다시 보고한다
- ⚠️ σ는 **무조건부 풀링 분산**(2023~2026)이다. 이벤트 조건부 분산은 다를 수 있다
- ⚠️ **무조건부 30거래일 평균이 +2.4%** 다(표본 측정 +1.8%에서 상승). raw CAR 3%는 이 유니버스의
  기본 드리프트보다 **0.6%p 위일 뿐**이므로, **팩터·시장 조정 없는 CAR로는 아무 결론도 못 낸다**

### 5.3 1층 kill

> 실현 가능 CAR의 비관 경계가 §5.2 임계에 미달하면 **프로젝트 종료. 인덱스/DCA로 귀결.**

---

## 6. 검정 방법 — 2층

| 항목 | 값 |
|---|---|
| 방식 | walk-forward 확장창, **주 표본 구간 안에서만** |
| 벤치마크 | **IWM / IWC 우선**, IJS·SPY 참고 |
| 비용 | 종목별 EDGE 스프레드 실측 기반 재추정 |
| capacity | $1k / $10k / $100k에서 각각 net 초과. **x축은 신호 공급이 아니라 ADDV**(§1.2) — 건당 주문금액 ÷ ADDV를 계좌 규모별로 표로 낸다. 실측 기준점: $35/$200k = **0.02%**($700) vs $5,000/$200k = **2.5%**($100k) |
| 경로 분산 | **부트스트랩 경로 시뮬레이션.** 중앙값·5%·95% 분위를 평균과 함께 보고 |
| 다중검정 | **Deflated Sharpe / PBO** — §4의 신고 N으로 계산 |
| 기타 | 비겹침 샘플 + purged/embargo CV, turnover 리포트, 양도소득세 22% 주석 |

**성공 기준 (2층)** — 아래를 **모두** 만족. 정도 표현은 쓰지 않는다:

1. net·편향보정 후 **IWM 대비 연 초과 > 0** (그리고 IWC 대비도 > 0)
2. **MDD가 동일 구간 IWC의 MDD를 넘지 않는다.**
   판정 구간은 **주 표본(2023~2026) 안의 최대 낙폭 구간**이다 — 2022는 주 표본 밖이므로
   **walk-forward 폴드가 아니라 별도 보조 검정**이며 결과는 "참고"로만 표시한다
3. capacity: **$1k·$10k·$100k 세 점 모두에서 net 초과 > 0**
4. 부트스트랩 **5% 분위 > −50%** — **대상**: `$10k 계좌 · 주 표본 전 구간 누적 수익률` 경로 분포

**통계적 유의성은 2층에서 요구하지 않는다** (1층에서 이미 물었으므로).

**관측은 1회.**

---

## 7. 단계별 kill 조건과 시간 상한

시간 상한은 **작업일** 기준이며 합계 18일로 §0의 "총 15~20 작업일" 안에 든다.
**상한을 넘기면 그 단계를 중단하고 재계획을 커밋한다** — 조용히 연장하지 않는다.

| 단계 | kill 조건 | 시간 상한 | 상태 |
|---|---|---|---|
| 0 — 집행가능성 게이트 | Toss 취급률 < 60% | — | ✅ **통과 90.7%** |
| 0.5 — 사전등록 | kill 없음. **이 파일 커밋 전에는 단계 2 이후 진행 금지** | — | 이 커밋 |
| **1 — 커버리지 + 보유기간 내 퇴출 측정** | **(1)** 최근 구간도 커버리지 임계(80%) 미달 → 종료<br>**(2)** **보유기간(H=90) 내 퇴출 신호가 있는 결측 이벤트 비율 > 15%** → 종료 | **3일** | ✅ **통과 — 커버리지 84.5% · 퇴출 3.1%** |
| 2 — DERA 45분기 일괄 수집 | kill 없음 | **1일** | |
| 3 — 이벤트스터디 [1층] | 실현 가능 CAR **비관 경계**(h=0)가 §5.2 임계 미달 (**판정 t는 winsor 1% + 교차상관 보정**) | **5일** | |
| 3.5 — 지정가·정수주 실주문 | **kill 없음.** 1층 통과 후에만. 실제 돈이 나가므로 **사용자 승인 필수**이고, **주문 취소 엔드포인트 확보가 선행조건** — 확보 실패 시 시작하지 않는다 | **4일**<br>(단계 4와 공유) | |
| 4 — 이벤트드리븐 하네스 | kill 없음 | ↑ | |
| 5 — [2층] | §6 성공 기준 미달 | **5일** | |
| 6 — 실전 이행 | 조건부. kill 없음 | 별도 | |

> ⚠️⚠️ **폐기된 kill 조건 주의.** 이전 판의 단계 1 kill (2)는 **"인수 공시 간격 p50 > 90일"** 이었고
> **측정값 562일(전수 518일)로 발동했다.** §3.3의 이탈 기록(D-1)에 따라 위 조건으로 **교체**됐다.
> **구 조건을 되살리지 말 것** — 되살리면 프로젝트가 즉시 종료된다.
>
> ⚠️ **15%는 문헌 근거 없는 프로젝트 고유 임의값이다.** WWC attrition standard는 RCT의 차등 감쇠용이고
> 우리에겐 처치군이 없어 정의되지 않는다(`docs/prereg-deviation-and-attrition-research.md` §4.2).
> **정당화는 감도로만 한다** — 전수 측정값 3.1%와 여유가 커서 10~20% 어디로 옮겨도 결론이 안 바뀐다.

---

## 9. 정정 기록 (Amendments)

원본은 커밋 `f960342`에 남아 있고 이 절은 그 이후의 변경을 타임스탬프와 함께 기록한다.
**스펙을 바꾼 것이 아니라 구현이 스펙을 위반한 것을 고친 경우도 여기 적는다** — 보고된 숫자가
움직이면 독자가 알아야 하기 때문이다.

### A-1 (2026-08-15) — 음수 공시지연 필터 누락

| 필드 | 값 |
|---|---|
| **성격** | **이탈 아님. 구현 버그 수정** — 스펙(§1.2 "공시지연 ≤5영업일", §7 단계 2 검증 "미래 누수 0건")은 그대로다 |
| **무엇이 틀렸나** | 필터가 `lag > 5`만 봐서 **음수 지연이 통과**했다. DERA 원자료에 `TRANS_DATE > FILING_DATE` 인 오기가 45분기에 54 거래행 있다 |
| **왜 문제인가** | 진입 시각은 `FILING_DATE` 기준이라 진입 자체는 안전하다. 그러나 **거래단가가 공시 이후 거래의 것**이 되어 §1.4 선택 규칙(`price deviation = (공시일 종가 − 거래단가)/거래단가`)이 오염된다 |
| **발견 경위** | 단계 2 검증 (2)를 실행하자 원자료 54건이 잡혔고, 우리 이벤트 집합에 7건이 남아 있음이 드러났다 |
| **영향 — 주 표본** | 이벤트 **14,143 → 14,136**(−7, 0.05%) · 주 스펙 판정 표본 **2,356 → 2,355**(−1) |
| **영향 — 45분기** | 이벤트 60,399 → **60,378**(−21) |
| **판정에 미친 영향** | **없다.** 커버리지 84.5% · 퇴출 3.1% · σ 23.2% · **t = 3.69** · ADDV 잔존 70.4% · 부과 스프레드 p50 1.11% — 보고 자릿수에서 전부 동일하다 |
| **시점** | **어떤 CAR도 계산하기 전.** 단계 2 수집 직후 |

수정 후 재검증: 이벤트 집합 음수 지연 **0건 / 60,378** ✅ · 접기 비율 2.74배 ✅ ·
`AFF10B5ONE` 부재 32분기 KeyError 없음 ✅ · PIT 스팟체크 5건 전부 공시일 이하 ✅


### A-2 (2026-08-16) — 단계 3 명세 확정: 생존편향 분모·측정 정의 8건

**성격: 명세 보강.** 단계 3 착수 전 점검에서 **판정에 필요한 정의가 빠져 있음**이 드러났다.
빠진 채로 계산하면 그 자리가 전부 사후 자유도가 된다. **어떤 CAR도 계산하기 전**에 확정한다.

#### (1) ★★ 생존편향 — 우리 파이프라인이 폐지 종목을 조용히 버리고 있었다

`gen_microcap_tradable.py`가 Toss **`status == DELISTED`** 종목 192개를 배제하는데 이건
**현재 시점 상태**다. 2023년에 거래되던 회사가 2025년에 폐지되면 그 회사의 2023년 이벤트가
통째로 사라진다 — **미래를 보고 표본을 고르는 것**이다.

```
배제로 사라진 이벤트                                863건 (필터 통과의 6.1%)
  → 공시일 < 폐지일 (당시엔 거래 가능했다)          693건
  → 그중 PIT 시총 <$300M (판정 밴드)               179건 / 39종목
  → 그 179건의 가격 패널 보유                        0건  ← Yahoo가 이력을 지운 코호트(§3.1)
```

**이것이 §3.2를 무력화하고 있었다.** 결측 경계는 결측을 **분모에 넣어야** 작동하는데,
이 179건은 분모에 들어오기 전에 사라졌다. 즉 `h=0` 비관 경계가 **아무것도 깎지 않고 있었다.**

**확정 — 판정 모집단을 다음으로 정의한다:**

```
판정 모집단 = 필터 통과 ∩ PIT 시총 <$300M ∩ ADDV >= $200k
              ∩ Toss 취급 ∩ (status == ACTIVE  OR  filing_date < delistDate)
관측  = 위 중 가격이 있는 것        (현재 2,355건)
결측  = 위 중 가격이 없는 것        (현재 179건 — ADDV를 산출할 수 없으므로 전량 포함)
```

- 결측에 `E[결측] = 0.031 × (−55%) + 0.969 × (관측 CAR × h)`, **판정 `h = 0`** 적용
- **179건을 전량 결측으로 세는 것이 보수적 선택이다.** 가격이 없어 ADDV를 못 재므로
  "ADDV 통과율 70.4%를 곱해 약 126건"으로 줄이는 처리도 가능하나, 그쪽이 덜 보수적이다.
  **판정은 179건 전량**, 126건 가정은 **감도로만 병기**한다
- 그 결과 판정 커버리지 = 2,355 / 2,534 = **92.9%**, 보정식은
  `보정 CAR = 0.929 × CAR + 0.071 × (−1.705%) = 0.929 × CAR − 0.121%p`
  → **임계 3.0%를 넘으려면 관측 CAR이 약 3.36% 필요**하다. 이전에는 이 해감이 0이었다
- `delistDate`가 없는 40종목은 시점을 알 수 없으므로 **계속 배제**하고, 배제 건수를 보고한다

#### (2) 측정 정의 7건 — 빠져 있던 자유도

| 축 | 확정값 | 근거 |
|---|---|---|
| **호라이즌 단위** | **거래일** (H=30은 30거래일) | §5.2 역산이 `252/H`를 쓰므로 거래일이 함의였다. 명시한다 |
| **누적 방식** | **BHAR**(보유기간 초과수익) = `(1+실현수익) − (1+팩터모형 기대수익)`. 일별 합산 CAR은 **참고 병기**(N 미계상) | §5.2.1의 σ가 **30거래일 보유수익률 분포**에서 나왔다. 일별 합산으로 판정하면 **t = 3.69의 근거와 측정 단위가 어긋난다.** 실제 계좌가 겪는 것도 보유수익이다 |
| **추정 창** | **t−250 ~ t−31 거래일**, 최소 100관측. 미달이면 그 이벤트는 **결측으로 분류**(위 (1)의 분모에 포함) | 이벤트 전후 오염을 피하는 표준 구성. 최소 관측 미달을 관측으로 세면 추정 잡음이 CAR로 샌다 |
| **진입 / 청산** | 진입 **t+1 시가**, 청산 **t+1+H 종가** | 진입만 고정돼 있었다. Form 4는 대부분 장마감 후 제출된다 |
| **비용 부과** | 왕복 = 수수료 0.10% × 2 + `spread_final` × 2 | §5.2와 동일. 지정가 완화는 단계 3.5 전까지 가정하지 않는다 |
| **winsorize 1% 적용 지점** | **이벤트별 BHAR의 횡단면 양측 1%** | 사전등록은 "winsor 1% 양측"만 있고 대상이 없었다. 검정력 표의 σ와 측정 단위가 같은 지점이 여기다 |
| ~~시장 국면 더미~~ | **삭제** | 정의도 경계도 없었다. **방향·정의가 없다는 이유로 52주 고가 거리를 폐기한 기준**을 일관되게 적용한다. 역주기성은 FF5 + MOM + IWC가 흡수한다 |

#### (3) 가격 소스

**`data/qlib_us_small`**(OHLC, 2026-08-14까지, 주 스펙 457종목 중 456 보유)를 쓰고
**배당·분할 조정 수익률**(`$factor`)로 계산한다.

- `data/candidate_closes.csv`는 **종가만** 있고 **2026-04-06에서 끊긴다** — `t+1 시가` 진입을
  구현할 수 없고 H=90의 10.5%가 우측 절단된다. 판정에 쓰지 않는다
- 배당을 빼면 CAR이 체계적으로 낮게 나온다 — **판정 표본의 24.3%가 은행**이다(§9 A-2 (4))
- qlib 번들에 없는 1종목은 결측으로 분류한다

#### (4) 진단 보고 2건 (판정 아님 · N 미계상)

**섹터 집중이 예상과 달랐다.** 주 스펙 이벤트의 **State Commercial Banks 17.9% ·
National Commercial Banks 6.4% · REIT 5.1% · 보험 4.5% = 금융 약 34%** 이고 바이오텍은 8.3%로 3위다.
주 표본 구간이 **2023 지역은행 위기**와 겹치므로 신호가 "insider alpha"가 아니라
**"지역은행 낙폭 리바운드"** 일 수 있다 — §5의 역주기성 우려의 구체적 형태다.

→ ① **SIC 섹터 더미 통제**(이미 §5에 있음) ② **은행 제외 부표본 BHAR 병기**.
②는 결과가 한 섹터에 끌려갔는지 드러내는 진단이며 **고를 수 없으므로 §4.1에 따라 N에 세지 않는다**
(NANO 층화와 같은 성격). **판정은 전체 표본으로만 한다.**



---

## 10. 판정 결과 — 1층 게이트 (2026-08-16)

**관측 1회. 주 스펙(N #1) 단일 판정이며, 이 결과를 보고 스펙을 바꾸지 않는다.**

### 10.1 주 스펙 판정

| 항목 | 값 |
|---|---|
| 표본 | 관측 **2,247** · 결측 179 · 커버리지 **92.6%** |
| 총 BHAR (비용 전) | **+0.11%** (t = 0.26) |
| 비용 (왕복) | **−2.86%** (수수료 0.20% + 스프레드 2×1.11% 중앙) |
| 실현 가능 BHAR (비용 후, winsor 1%) | **−3.17%** |
| **h=0 비관 경계** | **−3.06%** |
| **임계** (net 10%/yr · H=30 · 실측 스프레드) | **+3.60%** |
| 교차상관 보정 t | **−3.84** (하한 +3.0) |

### 10.2 §5.3 kill 조건 발동

> 실현 가능 BHAR의 비관 경계(**−3.06%**)가 §5.2 임계(**+3.60%**)에 **미달한다.**
> **1층 게이트 불통과 → 프로젝트 종료. 인덱스/DCA로 귀결.**

**비용 문제가 아니다.** 비용 전 총 BHAR이 **+0.11%(t=0.26)** 로 0과 구분되지 않는다.
스프레드가 0이어도 임계는 `0.10 × 30/252 = 1.19%` 이고 관측치는 그 **9분의 1**이다.
집행을 아무리 개선해도 넘지 못한다.

### 10.3 진단 (판정 아님 · N 미계상)

| 진단 | 결과 |
|---|---|
| **모멘텀 버킷** | 최상위(공시 전 20일 수익률 >10%)만 **+3.53%(t=2.54)**, 나머지는 0 또는 음수. **원논문의 "최상위 버킷이 가장 높다"는 방향은 재현**됐으나 중간 구간 단조성은 없다. **그 최상위 버킷조차 비용 후 +0.06%** — 선별해도 본전이다 |
| NANO 층화 | +0.92% vs 비NANO +0.06%. 결과가 나노캡에 끌려가지 않았다 |
| 은행 제외 | 은행 +0.66%(t=1.61) · 비은행 −0.16%. **은행 리바운드가 결과를 만들지 않았다** — 오히려 은행이 약간 나았고 둘 다 0 근처다 |
| raw vs winsor 1% | −2.75%(t=−6.65) vs −3.17%(t=−9.52). 방향 동일 |
| calendar-time (D16 교차확인) | 일별 alpha +0.044%(연 +11.1%), **t=1.59로 무의미**. 이벤트 단위와 **같은 부호**이며 결론이 뒤집히지 않는다 |
| 교차상관 실측 | ρ̄ = **0.0234** · n̄ = 179 · 팽창 **2.27배** — §5.2.1이 가정한 1.7배보다 **34% 크다**. 즉 사전 검정력 추정은 낙관이었다 |

### 10.4 이 판정이 답한 것

**"실현 가능한 selection alpha가 존재하는가"에 대한 답은 아니오다.**
얻은 것은 편향 없는 알파 추정치가 아니라 §4가 말한 더 약하지만 정직한 명제다 —
**측정된 편향 폭과 실제 집행 비용을 감안하면 신호가 남지 않는다.**

문헌값(최상위 버킷 30일 CAR 6.3%)과의 차이는 세 가지로 설명된다:
① 문헌은 **팩터·시장 조정을 하지 않은 raw CAR**이다 — 우리 표본의 raw 30일 보유수익은 **+2.15%**이고
   팩터모형 기대수익이 **+2.05%** 라 조정 후 남는 것이 +0.11%다
② 문헌은 **생존편향을 다루지 않는다**(원논문이 직접 명시)
③ 문헌은 실효 스프레드 2%를 가정하고도 비용 후 3.3%가 남는다고 했으나, 우리 실측 스프레드
   중앙값 1.11%로도 **비용 전 신호가 없어** 비용 논의 이전에 끝난다

---

## 8. 서명

이 문서에 적힌 값 외의 스펙으로 판정을 내리지 않는다.
추가 시도가 필요해지면 **금지가 아니라 §4의 N을 재신고하고 다시 커밋**한다.

| | |
|---|---|
| 확정자 | **SANGWAN** |
| 확정일 | **2026-08-15** |
| 근거 데이터 | 단계 1 전수 확정 — DERA 2023q1~2026q1 13분기, 이벤트 14,136, 주 스펙 2,355 (§9 A-1 반영) |
| 이탈 기록 | **D-1**(kill 조건 재명세) · **D-2**(스프레드 합격선) — 둘 다 승인됨. 삭제하지 않는다 |
| 정정 기록 | **A-1**(음수 공시지연 필터 누락, 판정 영향 없음) · **A-2**(단계 3 명세 확정 — 생존편향 분모 + 측정 정의 8건) — §9 |
